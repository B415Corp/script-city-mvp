# 🚀 План реализации автоматической системы регистрации ECS

## 📋 Обзор

Этот план описывает внедрение **автоматической системы регистрации** для ECS компонентов, систем, кластеров и фабрик сущностей. Основная цель - создать единый флоу разработки, где создание компонента/системы в одном месте автоматически приводит к их регистрации во всех необходимых местах.

## 🎯 Ключевые преимущества

- **📍 Единый флоу**: Создал файл → автоматически зарегистрировано
- **🔒 Безопасность**: Невозможно забыть зарегистрировать компонент
- **🧪 Тестируемость**: Реестры легко изолировать для тестов
- **📈 Масштабируемость**: Добавление новых типов не требует изменения core
- **🔍 Отладка**: Все компоненты/системы в одном месте для анализа

## 🏗️ Архитектура

### Глобальные реестры

- `ComponentRegistry` - реестр всех компонентов
- `SystemRegistry` - реестр всех систем с метаданными
- `ClusterRegistry` - реестр кластеров систем
- `EntityFactoryRegistry` - реестр фабрик сущностей

### Умные конструкторы

- `createComponent()` - создает компонент + автоматически регистрирует
- `createSystem()` - создает систему + автоматически регистрирует с метаданными
- `createCluster()` - создает кластер + автоматически регистрирует
- `createEntityFactory()` - создает фабрику + автоматически регистрирует

---

## 📅 План реализации (Phase-based)

## Phase 1: Базовые реестры и компоненты ✅

### 1.1 Создание базовых реестров

**Файлы:**

- `src/core/ecs/registry/component_registry.ts`
- `src/core/ecs/registry/system_registry.ts`
- `src/core/ecs/registry/cluster_registry.ts`
- `src/core/ecs/registry/entity_factory_registry.ts`

**Задачи:**

- Создать классы реестров с методами `register()`, `get()`, `getAll()`
- Добавить типы для метаданных систем и кластеров
- Реализовать паттерн Singleton для глобального доступа

### 1.2 Умные конструкторы

**Файлы:**

- `src/core/ecs/core/smart_constructors.ts`

**Задачи:**

- `createComponent(name, schema)` - создает компонент через `defineComponent()` + регистрирует в `ComponentRegistry`
- `createSystem(name, components, updateFn, metadata)` - создает систему + регистрирует в `SystemRegistry` с метаданными
- Интеграция с существующими `defineComponent` и фабриками систем

### 1.3 Автоматическая регистрация в ECSManager

**Файлы:**

- `src/core/ecs/ecs_manager.ts` (модификация)

**Задачи:**

- Добавить методы `autoRegisterComponents()`, `autoRegisterSystems()`
- Заменить ручную регистрацию на автоматическую через реестры
- Сохранить обратную совместимость для существующих компонентов

**Результат Phase 1:**

```
✅ Компоненты: createComponent() → автоматически зарегистрирован
✅ Системы: createSystem() → автоматически зарегистрирована
❌ Кластеры: еще ручная настройка
❌ Интервалы: еще ручная настройка
❌ Events: еще ручная настройка
```

---

## Phase 2: Расширенные метаданные систем

### 2.1 Метаданные для систем

**Файлы:**

- `src/core/ecs/registry/system_registry.ts` (расширение)

**Задачи:**

- Добавить интерфейс `SystemMetadata`:

  ```typescript
  interface SystemMetadata {
    name: string;
    cluster?: string; // Кластер принадлежности
    interval?: number; // Интервал выполнения
    eventTriggers?: string[]; // События-триггеры
    enabled?: boolean; // Включена по умолчанию
    dependencies?: any; // Зависимости для создания
  }
  ```

- Расширить `SystemRegistry` методами:
  - `getSystemsByCluster(clusterName)`
  - `getIntervalSystems()`
  - `getEventDrivenSystems()`

### 2.2 Автоматическое создание кластеров

**Файлы:**

- `src/core/ecs/registry/cluster_registry.ts`

**Задачи:**

- Метод `autoCreateFromSystemMetadata()` - создает кластеры на основе метаданных систем
- Интеграция с `ECSManager.initializeClusters()`

### 2.3 Интервальные системы

**Файлы:**

- `src/core/ecs/ecs_manager.ts` (расширение)

**Задачи:**

- Поддержка интервалов в `updateSystems()` для индивидуальных систем
- Интеграция с существующими кластерными интервалами

**Результат Phase 2:**

```
✅ Компоненты: createComponent() → автоматически зарегистрирован
✅ Системы: createSystem() → автоматически зарегистрирована
✅ Кластеры: createSystem(..., {cluster: 'population'}) → автоматически в кластер
✅ Интервалы: createSystem(..., {interval: 300}) → автоматически с интервалом
❌ Events: еще ручная настройка
```

---

## Phase 3: Event-driven системы

### 3.1 Event система в ECSManager

**Файлы:**

- `src/core/ecs/ecs_manager.ts` (расширение)

**Задачи:**

- Добавить `eventSystemMap` для маршрутизации событий к системам
- Реализовать `handleEventSystem()` и `handleCallSystem()`
- Подписаться на `Events.CallSystem` и кастомные события

### 3.2 Расширение умных конструкторов

**Файлы:**

- `src/core/ecs/core/smart_constructors.ts` (расширение)

**Задачи:**

- Добавить поддержку `eventTriggers` в `createSystem()`
- Автоматическая подписка на события при регистрации системы

### 3.3 Интеграция с существующими event-системами

**Файлы:**

- `src/core/ecs/systems/clusters/day_night_cycle_system.ts` (модификация)

**Задачи:**

- Перевести существующие event-driven системы на новый API
- Заменить ручной `eventBus.emit()` на декларативные `eventTriggers`

**Результат Phase 3:**

```
✅ Компоненты: createComponent() → автоматически зарегистрирован
✅ Системы: createSystem() → автоматически зарегистрирована
✅ Кластеры: createSystem(..., {cluster: 'population'}) → автоматически в кластер
✅ Интервалы: createSystem(..., {interval: 300}) → автоматически с интервалом
✅ Events: createSystem(..., {eventTriggers: ['work:start']}) → автоматически на события
```

---

## Phase 4: Фабрики сущностей и завершение

### 4.1 Entity Factory Registry

**Файлы:**

- `src/core/ecs/registry/entity_factory_registry.ts`

**Задачи:**

- Реестр фабрик сущностей с автоматической регистрацией
- Интеграция с `EntityFactory` в `ECSManager`

### 4.2 Умный конструктор фабрик

**Файлы:**

- `src/core/ecs/core/smart_constructors.ts` (расширение)

**Задачи:**

- `createEntityFactory(name, definition)` - создает фабрику + регистрирует
- Поддержка builder pattern для сложных сущностей

### 4.3 Финальная интеграция

**Файлы:**

- `src/core/core.ts` (расширение)

**Задачи:**

- Добавить высокоуровневый API в `Core` класс
- Обеспечить доступ к реестрам для отладки и статистики

---

## 📁 Структура файлов

```
src/core/ecs/
├── registry/
│   ├── component_registry.ts       # Реестр компонентов
│   ├── system_registry.ts          # Реестр систем с метаданными
│   ├── cluster_registry.ts         # Реестр кластеров
│   └── entity_factory_registry.ts  # Реестр фабрик сущностей
├── core/
│   ├── smart_constructors.ts       # Умные конструкторы
│   └── component_schema.ts         # (существующий)
├── ecs_manager.ts                  # (расширить автоматической регистрацией)
└── components/
    └── */*.ts                      # Использовать createComponent()
```

---

## 🔄 Миграция существующего кода

### Компоненты

```typescript
// Было:
export const Person = defineComponent('Person', {
  /* schema */
});

// Стало:
export const Person = createComponent('Person', {
  /* schema */
});
```

### Системы

```typescript
// Было: ручная регистрация в ECSManager
const workSystem = createWorkSystem(timeService);
this.scheduleManager.registerSystem(workSystem);

// Стало:
export const WorkSystem = createSystem('work', [...], updateFn, {
  cluster: 'population',
  dependencies: { timeService: true }
});
```

### Кластеры

```typescript
// Было: ручная настройка в clustersRegistry
const clustersRegistry = {
  population: { systemNames: ['Work', 'Movement'], enabled: true },
};

// Стало: автоматическое создание из метаданных систем
// Или явное:
export const PopulationCluster = createCluster('population', ['work', 'movement'], {
  enabled: true,
  description: 'Population simulation systems',
});
```

---

## ✅ Критерии успеха

### Функциональность

- [ ] Все компоненты автоматически регистрируются
- [ ] Все системы автоматически регистрируются с правильными метаданными
- [ ] Кластеры создаются автоматически или явно
- [ ] Интервальные системы работают корректно
- [ ] Event-driven системы реагируют на события
- [ ] Фабрики сущностей регистрируются автоматически

### Качество кода

- [ ] Полная типобезопасность
- [ ] Отсутствие дублирования кода
- [ ] Легкая тестируемость реестров
- [ ] Подробное логирование для отладки

### DX (Developer Experience)

- [ ] Единый флоу: создал → работает
- [ ] Ясные ошибки при неправильном использовании
- [ ] Автодополнение в IDE
- [ ] Документация для каждого API

### Производительность

- [ ] Минимальный overhead от реестров
- [ ] Эффективная маршрутизация событий
- [ ] Оптимизированные интервальные обновления

---

## 🧪 Тестирование

### Unit тесты

- `ComponentRegistry` - регистрация и получение компонентов
- `SystemRegistry` - метаданные и фильтрация систем
- `ClusterRegistry` - автоматическое создание кластеров
- Умные конструкторы - правильная регистрация

### Integration тесты

- Полная инициализация ECS через реестры
- Автоматическая настройка кластеров и интервалов
- Event-driven системы и маршрутизация

### E2E тесты

- Создание сущностей через новый API
- Выполнение систем в правильном порядке
- Правильная обработка интервалов и событий

---

## 📈 Метрики успеха

- **Время на создание компонента**: уменьшилось с 5 минут до 2 минут
- **Количество ручной регистрации**: уменьшилось с 20+ мест до 0
- **Ошибки забытой регистрации**: 0 случаев
- **Скорость добавления новых систем**: увеличилась в 3 раза
- **Покрытие тестами реестров**: 100%

---

## 🎯 Следующие шаги

1. **Начать с Phase 1** - базовые реестры и компоненты
2. **Тестировать каждую фазу** перед переходом к следующей
3. **Документировать API** по мере реализации
4. **Мигрировать существующий код** постепенно
5. **Собрать обратную связь** от команды разработки

---

_Этот план обеспечивает плавный переход к автоматической системе регистрации, сохраняя обратную совместимость и улучшая DX разработчиков._
