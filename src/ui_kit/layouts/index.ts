/**
 * Экспорты layout компонентов UI Kit.
 *
 * **Теги**: `tech:phaser`, `arch:ui`, `arch:ui-kit`
 */

export { UIContainer } from './ui_container';
export type {
  UIContainerConfig,
  UIContainerChild,
  UIContainerChildConfig,
  UISizeMode,
} from './ui_container';
