import Phaser from 'phaser';

/**
 * Сцена главного меню
 * Теги: arch:ui, tech:phaser
 */
export class MenuScene extends Phaser.Scene {
  constructor() {
    super({ key: 'MenuScene' });
  }

  create(): void {}
}
